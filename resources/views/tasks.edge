@layout('main')
@section('content')
    <h1>{{title}}</h1>
    @if(old('notifications'))
        <div class="alert alert-success" role="alert">
            {{old('notifications')}}
        </div>
    @endif
    <div class="list-group">
        @each(item in tasks)
            <a href="#" class="list-group-item list-group-item-action">{{item.titulo}}</a>
        @endeach
    </div>
@endsection